'use strict';

const should = require('should');
const { Compiler } = require('djantajs-compiler-core');

describe('Single bundle packaging generator', () => {

  it('should compiler instance be Ok', () => {
    //should(Compiler).be.ok();
  });
});
